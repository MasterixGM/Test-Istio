apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test
  namespace: test
spec:
  project: default
  source:
    repoURL: 
    path: apps
    targetRevision: HEAD
  destination:
    name: in-cluster
  syncPolicy:
    automated:
      prune: false
      selfHeal: true